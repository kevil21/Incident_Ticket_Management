Create three GitHub branches: dev, stage, and main.
Clone the repository to your local environment.
Develop a Python script that cleans and uploads the data into a pre-created Elasticsearch index.
Verify the data ingestion by creating a Kibana visualization dashboard for the incident data.
Once the workflow is tested and confirmed, promote the changes from dev → stage → main branches using pull requests.